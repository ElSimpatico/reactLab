/** Car model
 * @property { string } key Car identification
 * @property { string } name Car model name
 * @property { string } image Car model image
 * @property { string } price Car model price
 */
export interface Car {
    key: string;
    name: string;
    image: string;
    price: string;
}
